# Static data API (generated by ingest/api.ts into public/api/).
# Short max-age so clients pick up a nightly rebuild quickly; the payloads carry their
# own signature, so a stale-by-minutes copy is harmless.
/api/*
  Cache-Control: public, max-age=300, must-revalidate
  Access-Control-Allow-Origin: *
